# ТЗ: «Охота на лис» — десктопное приложение (fx_python)

## Цель
Десктопная версия игры «Охота на лис» на Python с окном и возможностью
сохранения/продолжения игры.

## Стек
- Python 3.14, GUI — Tkinter (стандартная библиотека, без внешних пакетов)
- Сохранения — JSON-файлы

## Функциональность
1. **Новая игра:** размер поля 8/9/10/15, лисы 5..размер (по умолчанию 5, для 15 — 10),
   лимит 100 ходов.
2. **Игровое поле:** сетка кнопок. Клик открывает клетку и показывает «пеленг» —
   число лис на вертикали/горизонтали/диагоналях. Клетка с лисой помечается 🦊.
3. **Серые линии:** открытая клетка с пеленгом 0 закрашивает серым всю свою
   вертикаль, горизонталь и обе диагонали.
4. **Конец игры:** победа (все лисы найдены) или поражение (лимит ходов).
5. **Сохранение:** кнопка «Сохранить» пишет состояние игры (размер, лисы, ходы,
   открытые клетки, найденные лисы, статус) в JSON-файл (диалог выбора файла,
   по умолчанию savegame.json).
6. **Загрузка:** кнопка «Загрузить» читает сохранение и продолжает игру с того же
   места.

## Критерий приёмки
- Приложение запускается окном и в игру можно играть мышью.
- Сохранение создаёт корректный JSON; после загрузки состояние игры идентично
  (проверяется тестом).
- Логика игры покрыта тестами (создание, пеленг, серые линии, победа, поражение,
  сохранение/загрузка) — все зелёные.

## Структура
- `game.py` — логика игры (класс `FoxGame`), не зависит от GUI
- `main.py` — GUI на Tkinter
- `test_game.py` — тесты
- `SPEC.md`, `DEV-JOURNAL.md`
