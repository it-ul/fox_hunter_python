# DEV-JOURNAL — fx_python

## Статус
Готово. Десктопная версия «Охота на лис» (Tkinter) с сохранением/загрузкой.

## Решения
- 2026-08-24: стек — Tkinter (встроен в Python 3.14, Tcl/Tk 9.0).
- Логика отделена от GUI (`game.py` + `main.py`) — логика покрыта тестами.
- Формат сохранения: JSON, версия 1 (поле `version` для совместимости).

## Проверка (2026-08-24)
- Тесты логики: 11/11 зелёные (создание, пеленг, серые линии, победа,
  поражение, save/load roundtrip, неверная версия, max_moves).
- Smoke-тест GUI: меню, доска, ход + перерисовка — ок.
- Исправление: клетки 15×15 были прямоугольными (Tk-кнопки: width/height в
  символах/строках). Переведено на Canvas — клетки рисуются прямоугольниками
  cell×cell в пикселях, гарантированно квадратные (проверено: 414×414 и
  510×510 для полей 9×9 и 15×15). Клик по координатам, выход за поле
  игнорируется.

## Изменения (2026-08-24)
- Лимит ходов: вместо 100 — `size² − 1` (свойство `FoxGame.max_moves`):
  8×8→63, 9×9→80, 10×10→99, 15×15→224. Сообщение и статистика обновлены.
- Курсор над клетками поля — «прицел» (`cursor="target"`: перекрестие с
  кружком). Примечание: Tk 9.0 на Windows НЕ поддерживает кастомные
  XBM/PhotoImage-курсоры (проверено) — используется встроенный "target".
- Защита `_update_foxes_options` от trace вне меню.

## Доработки по ToDo.md (2026-09-04)
- **ПКМ-пометка клеток**: правая кнопка красит закрытую клетку серым
  (как пеленг 0), повторный ПКМ снимает пометку. Пометки хранятся в игре
  (`marks`) и сохраняются в файл.
- **Результат над полем**: при победе/поражении сообщение выводится баннером
  НАД полем, поле остаётся на последнем ходе (отдельный финальный экран убран).
- **Иконка лисы**: сгенерирована (assets/fox.png), загружается в клетку вместо
  эмодзи; включена в сборку через FoxHunter.spec (datas). Файл с сервера был
  JPEG — сконвертирован в PNG.
- **Координаты поля**: слева цифры снизу вверх, снизу латинские буквы слева
  направо (a…, 1 снизу). Функция `coord_label` в game.py.
- **Окно управления** (`control_panel.py`): все кнопки (новая игра с выбором
  размера/лис, сохранить, загрузить, в меню, выход), информация о состоянии и
  **протокол игры** — список ходов вида «1. e4 — пеленг 2, лиса: нет».
  Протокол (`log`) и пометки сохраняются/загружаются. Главное окно показывает
  только поле (+баннер) и кнопку «☰ Управление».
- Тесты: 15/15 юнит + 19/19 UI-проверок.

## Дополнения и правки (2026-09-04)
- **SDI-интерфейс**: окно управления (теперь `ControlPanel(tk.Frame)`) и игровое
  поле объединены в одно окно — панель слева, поле справа. Отдельных окон нет.
- **Иконка приложения**: лиса (`assets/fox.png`) ставится в заголовок окна
  через `iconphoto`; та же иконка используется в клетках.
- **Стартовый экран**: добавлены описание и правила игры (в области поля).
- Главное окно: `frame`, `grid/pack`; убрана кнопка «☰ Управление» (панель всегда видна).
- Тесты: 15/15 юнит + 14/14 UI-проверок (SDI, иконка заголовка, правила, ПКМ,
  протокол, баннер).

## Раскладка окна в три колонки (2026-09-10)
- Главное окно: слева — описание и правила игры (видно постоянно), в центре —
  игровое поле (баннер результата + Canvas), справа — панель управления,
  информация и протокол ходов.
- Убрана кнопка «В меню» и метод `reset_to_menu`: новая игра запускается кнопкой
  «Начать игру», описание всегда на виду.
- Стартовый экран с правилами убран: до старта в центре — подсказка «Выберите
  размер поля … на панели справа и нажмите «Начать игру»».
- `ControlPanel` теперь встраивается в центральный контейнер (`__init__(app, parent)`);
  прежняя раскладка «панель слева / поле справа» заменена.
- Проверка: smoke-тест GUI (порядок колонок desc → board → ctrl, состав кнопок,
  старт игры) + тесты логики 15/15 зелёные.
- В описание (левая колонка) добавлены: расшифровка ЛКМ/ПКМ (левая/правая кнопка
  мыши) и пункт о сохранении текущей игры и её загрузке с продолжением с того же
  места. `.exe` пересобран, собран локальный zip-архив проекта.
- Правки панели и шапки (2026-09-10): селекты «Размер поля» и «Количество лис»
  прижаты к правому краю панели; шрифт заголовка окна 20 → 22; убран монохромный
  эмодзи-лис 🦊 из заголовка и титула окна (цветная иконка `assets/fox.png`
  осталась). `.exe` пересобран.
- Колонка описания переработана (2026-09-10): вместо текста правил — арт
  `assets/fox_art.png` (обрезан по содержимому, 250×278 px, прозрачный фон)
  и кнопка «Правила игры», открывающая всплывающее окно (`Toplevel`) с правилами
  (прокрутка, центрирование над главным окном, повторный клик поднимает уже
  открытое). В `FoxHunter.spec` — `datas=[('assets','assets')]`, чтобы арт попал
  в сборку. Проверка: smoke-тест GUI + логика 15/15.

## Запуск
`python main.py` — из папки fx_python.

## Публикация (2026-08-24)
- Собран .exe: `dist\FoxHunter.exe` (11,8 МБ, PyInstaller --onefile --windowed).
- GitHub-репозиторий: https://github.com/it-ul/fox_hunter_python (публичный)
- Release v1.0.0 с .exe:
  https://github.com/it-ul/fox_hunter_python/releases/download/v1.0.0/FoxHunter.exe
- Обновление: git push origin main (ветка main, теги через git push --tags).
